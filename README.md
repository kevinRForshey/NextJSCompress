To run the script copy the script into the root of your NextJS application and run the script from command line, powershell in Windows or any terminal to run the bash script in Linux.  To run the bash script in linux you will ned to make sure it is executable with chmod to change the permission or I guess you could do this through the gui and it will create a .zip file in that same directory which is what you want to upload the entire site to an LLM.
